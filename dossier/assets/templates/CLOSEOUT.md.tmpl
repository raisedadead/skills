# Phase <PHASE> Closeout — <slug>

Flavor: `<FLAVOR>`

Internal dossier note. This is scratchpad theater tooling, not a
package-manager changeset and not a public release note.

## Surface Delta

Observable interfaces that changed: routes, CLI flags, exported symbols,
schemas, resources, jobs, screens, model artifacts.

- <delta 1>
- <delta 2>

## Behavior Delta

Runtime behavior that changed without necessarily changing the surface:
performance, retry policy, errors, logs, layout, accessibility, outputs.

- <delta 1>
- <delta 2>

## Commits Landed

- `<hash>` — <subject> (PH<PHASE>-Bxx)
- `<hash>` — <subject> (PH<PHASE>-Bxx)

## Findings Closed

- B<n> <name> — <one-line summary>
- B<n> <name> — <one-line summary>

## Verification

- <command> — <result>
- <command> — <result>

## Rollout / Rollback Notes

- Rollout: <operator/user-owned action, if any>
- Rollback: <one-line recovery path, if any>

## Deferred

- B<n> (S3): <name> — <why deferred>
- B<n> (S4): <name> — <why deferred>

## External Artifacts Requiring User Approval

- Push / PR / publish / deploy: <needed? who owns?>
- Public release note / package changeset export: <needed? target path/tool?>
