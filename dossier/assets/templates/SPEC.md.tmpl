# Phase {PHASE} Spec

Flavor: `{FLAVOR}`

Compact active state for this phase. Keep it short enough to read every
task. Use `PLAN.md` for narrative planning and `AUDIT.md` for evidence.

## §G Goal

{one-line phase outcome}

## §C Constraints

- Per-task covenant: one commit, explicit path staging, sibling test or
  meta-gate bundled in same commit, `type(scope): subject` (no phase tag — `AUDIT.md §B` is the ledger).
- Scope names: {project units: packages, services, modules, surfaces}.
- Verification harness: {test/build/lint/contract commands}.
- Output rebaseline mode: {scoped / full / not applicable}.
- Push / PR / publish / deploy: user-owned only.
- Out of scope: {things this phase must not touch}.

## §I Interfaces

External surfaces this phase may touch. Preserve identifiers verbatim.

- api: {route / request / response}
- cmd: `{command}` -> {stdout/stderr/exit}
- file: `{path}` -> {schema / contract}
- env: `{VAR}` -> {required / optional}
- ui: {screen / component} -> {observable behavior}

## §V Invariants

Testable rules that must hold. Numbering is monotonic; never reuse IDs.

- V1: every changed unit has sibling behavior test, meta-gate, or recorded-output rebaseline in same commit
- V2: tasks are vertical slices through public interfaces, not layer-only edits
- V3: flaky single test -> skip + file finding, never raise project-wide retry count
- V4: long command >30s uses runtime adapter, never shell `sleep`
- V5: output rebaseline never runs before unbaselined diff is read
- V6: {phase-specific invariant}

## §T Tasks

Status: `.` todo / `~` wip / `x` done. Keep IDs monotonic.

| id | status | task | finding | cites |
| -- | ------ | ---- | ------- | ----- |
| T1 | . | {vertical slice through public interface} | B1 | V1,I.api |
| T2 | . | {next vertical slice} | C1 | V2,I.ui |

## §B Bugs / Findings

Compact backprop log. Sync details to `AUDIT.md` when evidence does not
fit here.

| id | date | cause | fix |
| -- | ---- | ----- | --- |
| B1 | {YYYY-MM-DD} | {root cause or risk} | {V-id / task / commit} |
