# Phase <N> — <slug>

Durable record for a service / app / infra / data / ML phase.
No semver header — services don't version like packages.
Operators read this on rollout / rollback / postmortem.

## Surface delta

What changed in observable interfaces — HTTP routes, CLI flags,
event schemas, DB columns, env vars, IAM scopes, terraform
resources, dbt models, model artefacts.

- <delta 1 — name the surface, e.g. `POST /v2/orders` request schema added `idempotency_key`>
- <delta 2 — e.g. column `users.tier` added (default `free`, backfilled in same migration)>
- <delta 3>

## Behaviour delta

Observable runtime changes that don't show in the surface —
performance, error rate, retry policy, log shape.

- <e.g. p99 ingest latency 320ms → 180ms (verified on staging)>
- <e.g. retry policy 3x exponential → circuit breaker, 5xx floor 0.5%>

## Migrations / rollouts performed

Steps the operator executed (or will execute) in order.

- <e.g. `db/migrations/0042_add_tier.sql` — additive, online, ~3 min on prod size>
- <e.g. feature flag `tier-gating` rolled to 10% → 100% over 48h>
- <e.g. `terraform apply -target=module.queue` — adds DLQ + IAM policy>

## Verification

How we know it worked. Each line cites a test / probe / dashboard.

- <e.g. contract tests pass against `openapi.v2.yaml` — see `tests/contract/`>
- <e.g. shadow traffic 1h: 0 diffs vs canary>
- <e.g. dashboard `<link>` shows 5xx flat across cutover>

## Findings closed

Finding-by-finding, named with B-id (links to AUDIT.md).

- B<n> <name> — <one-line summary>
- B<n> <name> — <one-line summary>

## Operational notes

Anything an on-call needs to know.

- Rollback procedure: <one-line + link>
- New alerts / dashboards: <names + links>
- New runbook entries: <link>

## Open at cut (deferred)

- B<n> (P3): <name> — <why deferred>
- B<n> (P4): <name> — <why deferred>
