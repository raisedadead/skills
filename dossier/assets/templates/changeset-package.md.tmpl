---
"<package-1>": patch
"<package-2>": patch
---

## What changed for consumers

Public-API-visible deltas. New / removed / renamed exports,
fixed defaults, new tokens / props / flags, removed
deprecations.

- <delta 1 — name the symbol>
- <delta 2>

## Test infrastructure

Internal but worth narrating. Coverage shifts, threshold floors,
new test layers, new meta-gates.

- coverage: <stmts %> / <branch %> / <funcs %> / <lines %> (was <baseline>)
- threshold floor: statements ≥ <N>, branches ≥ <N>, functions ≥ <N>, lines ≥ <N>
- new layers: L<n> <name> (count: <N> tests across <N> files)

## Polish

Finding-by-finding, named with B-id.

- B<n> <name> — <one-line summary>
- B<n> <name> — <one-line summary>

## Open at cut (deferred)

Items intentionally left for the next phase, with severity.

- B<n> (P3): <name> — <why deferred>
- B<n> (P4): <name> — <why deferred>
